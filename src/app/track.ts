//obsolete repetition of track- of 4 properties!
//also it should be ITrack... per naming convention

export interface Track {
    trackNumber: number;
    trackName: string;
    trackLength: string;
    trackPrice: number;
}
